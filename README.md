# week07_sharedprefrences

A Flutter project.

## Description

This Flutter project demonstrates how to store and retrieve data locally using the SharedPreferences package.

The app asks the user to enter their name and saves it on the device.
When the app is reopened, it remembers the user and displays a personalized welcome message.